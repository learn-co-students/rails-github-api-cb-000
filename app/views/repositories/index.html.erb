<div class = "container">
<h1><%= session[:username] %>'s Repos</h1>

  <%= form_tag("/repositories/create", method: "post") do %>
    <%= label_tag(:name, "Create a New Repo:") %>
    <%= text_field_tag(:name, nil, id: "new-repo") %>
    <%= submit_tag("Create") %>
  <% end %>

  <ul>
  <% @repos.each do |repo| %>
    <li><a href="<%= repo['html_url'] %>"><%= repo['name'] %></a>
        - <%= repo['description'] %>
    </li>
  <% end %>
  </ul>
</div>
